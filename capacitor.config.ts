import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'INVERNADERO-CONTROL-FRONTEND',
  webDir: 'www'
};

export default config;
